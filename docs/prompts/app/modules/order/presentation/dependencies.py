# order/presentation/dependencies.py from fastapi import Depends  from app.shared.database import get_session from ..application.use_cases import orderUseCases from ..infrastructure.repositories import PostgresorderRepository   async def get_order_use_cases(     session=Depends(get_session), ) -> orderUseCases:     """DI provider for order use cases."""     tenant_id = "00000000-0000-0000-0000-000000000001"  # TODO: from tenant context     repo = PostgresorderRepository(session, tenant_id)     return orderUseCases(repo=repo)