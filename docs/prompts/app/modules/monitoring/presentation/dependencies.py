# monitoring/presentation/dependencies.py from fastapi import Depends  from app.shared.database import get_session from ..application.use_cases import monitoringUseCases from ..infrastructure.repositories import PostgresmonitoringRepository   async def get_monitoring_use_cases(     session=Depends(get_session), ) -> monitoringUseCases:     """DI provider for monitoring use cases."""     tenant_id = "00000000-0000-0000-0000-000000000001"  # TODO: from tenant context     repo = PostgresmonitoringRepository(session, tenant_id)     return monitoringUseCases(repo=repo)