# accounting_gateway/presentation/dependencies.py from fastapi import Depends  from app.shared.database import get_session from ..application.use_cases import accounting_gatewayUseCases from ..infrastructure.repositories import Postgresaccounting_gatewayRepository   async def get_accounting_gateway_use_cases(     session=Depends(get_session), ) -> accounting_gatewayUseCases:     """DI provider for accounting_gateway use cases."""     tenant_id = "00000000-0000-0000-0000-000000000001"  # TODO: from tenant context     repo = Postgresaccounting_gatewayRepository(session, tenant_id)     return accounting_gatewayUseCases(repo=repo)