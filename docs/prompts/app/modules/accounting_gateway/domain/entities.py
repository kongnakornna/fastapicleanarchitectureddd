# accounting_gateway/domain/entities.py # Entities: AccountingSync from dataclasses import dataclass from datetime import datetime, timezone  from app.shared.base_entity import BaseEntity  from .exceptions import DomainError   def _utcnow() -> datetime:     return datetime.now(timezone.utc)   # TODO: Add domain entities for: AccountingSync # Example: # # @dataclass # class accounting_gatewayEntity(BaseEntity): #     code: str = "" #     name: str = "" # #     def __post_init__(self): #         self._validate() # #     def _validate(self) -> None: #         if not self.code: #             raise DomainError("Code is required")