# accounting_gateway/infrastructure/repositories.py from app.core.logging import logger from app.shared.exceptions import InfrastructureException   class accounting_gatewayRepositoryException(InfrastructureException):     code = "acg_REPO_ERROR"   class Postgresaccounting_gatewayRepository:     """Postgres repository for accounting_gateway (2-branch error handling)."""      def __init__(self, session, tenant_id: str):         self.session = session         self.tenant_id = tenant_id