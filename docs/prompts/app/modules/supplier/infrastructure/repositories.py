# supplier/infrastructure/repositories.py from app.core.logging import logger from app.shared.exceptions import InfrastructureException   class supplierRepositoryException(InfrastructureException):     code = "sup_REPO_ERROR"   class PostgressupplierRepository:     """Postgres repository for supplier (2-branch error handling)."""      def __init__(self, session, tenant_id: str):         self.session = session         self.tenant_id = tenant_id