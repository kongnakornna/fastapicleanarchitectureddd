# supplier/presentation/dependencies.py from fastapi import Depends  from app.shared.database import get_session from ..application.use_cases import supplierUseCases from ..infrastructure.repositories import PostgressupplierRepository   async def get_supplier_use_cases(     session=Depends(get_session), ) -> supplierUseCases:     """DI provider for supplier use cases."""     tenant_id = "00000000-0000-0000-0000-000000000001"  # TODO: from tenant context     repo = PostgressupplierRepository(session, tenant_id)     return supplierUseCases(repo=repo)