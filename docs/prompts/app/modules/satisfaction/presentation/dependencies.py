# satisfaction/presentation/dependencies.py from fastapi import Depends  from app.shared.database import get_session from ..application.use_cases import satisfactionUseCases from ..infrastructure.repositories import PostgressatisfactionRepository   async def get_satisfaction_use_cases(     session=Depends(get_session), ) -> satisfactionUseCases:     """DI provider for satisfaction use cases."""     tenant_id = "00000000-0000-0000-0000-000000000001"  # TODO: from tenant context     repo = PostgressatisfactionRepository(session, tenant_id)     return satisfactionUseCases(repo=repo)