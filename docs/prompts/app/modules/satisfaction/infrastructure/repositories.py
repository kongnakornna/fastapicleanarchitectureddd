# satisfaction/infrastructure/repositories.py from app.core.logging import logger from app.shared.exceptions import InfrastructureException   class satisfactionRepositoryException(InfrastructureException):     code = "csat_REPO_ERROR"   class PostgressatisfactionRepository:     """Postgres repository for satisfaction (2-branch error handling)."""      def __init__(self, session, tenant_id: str):         self.session = session         self.tenant_id = tenant_id