# irrigation/infrastructure/repositories.py from app.core.logging import logger from app.shared.exceptions import InfrastructureException   class irrigationRepositoryException(InfrastructureException):     code = "irr_REPO_ERROR"   class PostgresirrigationRepository:     """Postgres repository for irrigation (2-branch error handling)."""      def __init__(self, session, tenant_id: str):         self.session = session         self.tenant_id = tenant_id