# irrigation/presentation/dependencies.py from fastapi import Depends  from app.shared.database import get_session from ..application.use_cases import irrigationUseCases from ..infrastructure.repositories import PostgresirrigationRepository   async def get_irrigation_use_cases(     session=Depends(get_session), ) -> irrigationUseCases:     """DI provider for irrigation use cases."""     tenant_id = "00000000-0000-0000-0000-000000000001"  # TODO: from tenant context     repo = PostgresirrigationRepository(session, tenant_id)     return irrigationUseCases(repo=repo)