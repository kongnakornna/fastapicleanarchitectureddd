# forecast/infrastructure/repositories.py from app.core.logging import logger from app.shared.exceptions import InfrastructureException   class forecastRepositoryException(InfrastructureException):     code = "fc_REPO_ERROR"   class PostgresforecastRepository:     """Postgres repository for forecast (2-branch error handling)."""      def __init__(self, session, tenant_id: str):         self.session = session         self.tenant_id = tenant_id