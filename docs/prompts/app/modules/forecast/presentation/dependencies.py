# forecast/presentation/dependencies.py from fastapi import Depends  from app.shared.database import get_session from ..application.use_cases import forecastUseCases from ..infrastructure.repositories import PostgresforecastRepository   async def get_forecast_use_cases(     session=Depends(get_session), ) -> forecastUseCases:     """DI provider for forecast use cases."""     tenant_id = "00000000-0000-0000-0000-000000000001"  # TODO: from tenant context     repo = PostgresforecastRepository(session, tenant_id)     return forecastUseCases(repo=repo)