# backup/presentation/dependencies.py from fastapi import Depends  from app.shared.database import get_session from ..application.use_cases import backupUseCases from ..infrastructure.repositories import PostgresbackupRepository   async def get_backup_use_cases(     session=Depends(get_session), ) -> backupUseCases:     """DI provider for backup use cases."""     tenant_id = "00000000-0000-0000-0000-000000000001"  # TODO: from tenant context     repo = PostgresbackupRepository(session, tenant_id)     return backupUseCases(repo=repo)