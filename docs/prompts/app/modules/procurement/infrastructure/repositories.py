# procurement/infrastructure/repositories.py from app.core.logging import logger from app.shared.exceptions import InfrastructureException   class procurementRepositoryException(InfrastructureException):     code = "proc_REPO_ERROR"   class PostgresprocurementRepository:     """Postgres repository for procurement (2-branch error handling)."""      def __init__(self, session, tenant_id: str):         self.session = session         self.tenant_id = tenant_id