# procurement/presentation/dependencies.py from fastapi import Depends  from app.shared.database import get_session from ..application.use_cases import procurementUseCases from ..infrastructure.repositories import PostgresprocurementRepository   async def get_procurement_use_cases(     session=Depends(get_session), ) -> procurementUseCases:     """DI provider for procurement use cases."""     tenant_id = "00000000-0000-0000-0000-000000000001"  # TODO: from tenant context     repo = PostgresprocurementRepository(session, tenant_id)     return procurementUseCases(repo=repo)