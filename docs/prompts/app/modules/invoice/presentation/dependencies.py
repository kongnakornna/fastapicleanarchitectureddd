# invoice/presentation/dependencies.py from fastapi import Depends  from app.shared.database import get_session from ..application.use_cases import invoiceUseCases from ..infrastructure.repositories import PostgresinvoiceRepository   async def get_invoice_use_cases(     session=Depends(get_session), ) -> invoiceUseCases:     """DI provider for invoice use cases."""     tenant_id = "00000000-0000-0000-0000-000000000001"  # TODO: from tenant context     repo = PostgresinvoiceRepository(session, tenant_id)     return invoiceUseCases(repo=repo)