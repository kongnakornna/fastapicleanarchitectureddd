# gps/presentation/dependencies.py from fastapi import Depends  from app.shared.database import get_session from ..application.use_cases import gpsUseCases from ..infrastructure.repositories import PostgresgpsRepository   async def get_gps_use_cases(     session=Depends(get_session), ) -> gpsUseCases:     """DI provider for gps use cases."""     tenant_id = "00000000-0000-0000-0000-000000000001"  # TODO: from tenant context     repo = PostgresgpsRepository(session, tenant_id)     return gpsUseCases(repo=repo)