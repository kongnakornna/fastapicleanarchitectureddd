# gps/infrastructure/repositories.py from app.core.logging import logger from app.shared.exceptions import InfrastructureException   class gpsRepositoryException(InfrastructureException):     code = "gps_REPO_ERROR"   class PostgresgpsRepository:     """Postgres repository for gps (2-branch error handling)."""      def __init__(self, session, tenant_id: str):         self.session = session         self.tenant_id = tenant_id