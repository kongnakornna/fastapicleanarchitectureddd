# recommendation/presentation/dependencies.py from fastapi import Depends  from app.shared.database import get_session from ..application.use_cases import recommendationUseCases from ..infrastructure.repositories import PostgresrecommendationRepository   async def get_recommendation_use_cases(     session=Depends(get_session), ) -> recommendationUseCases:     """DI provider for recommendation use cases."""     tenant_id = "00000000-0000-0000-0000-000000000001"  # TODO: from tenant context     repo = PostgresrecommendationRepository(session, tenant_id)     return recommendationUseCases(repo=repo)