# payment/presentation/dependencies.py from fastapi import Depends  from app.shared.database import get_session from ..application.use_cases import paymentUseCases from ..infrastructure.repositories import PostgrespaymentRepository   async def get_payment_use_cases(     session=Depends(get_session), ) -> paymentUseCases:     """DI provider for payment use cases."""     tenant_id = "00000000-0000-0000-0000-000000000001"  # TODO: from tenant context     repo = PostgrespaymentRepository(session, tenant_id)     return paymentUseCases(repo=repo)