# payment/infrastructure/repositories.py from app.core.logging import logger from app.shared.exceptions import InfrastructureException   class paymentRepositoryException(InfrastructureException):     code = "pay_REPO_ERROR"   class PostgrespaymentRepository:     """Postgres repository for payment (2-branch error handling)."""      def __init__(self, session, tenant_id: str):         self.session = session         self.tenant_id = tenant_id