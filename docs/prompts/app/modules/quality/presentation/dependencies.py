# quality/presentation/dependencies.py from fastapi import Depends  from app.shared.database import get_session from ..application.use_cases import qualityUseCases from ..infrastructure.repositories import PostgresqualityRepository   async def get_quality_use_cases(     session=Depends(get_session), ) -> qualityUseCases:     """DI provider for quality use cases."""     tenant_id = "00000000-0000-0000-0000-000000000001"  # TODO: from tenant context     repo = PostgresqualityRepository(session, tenant_id)     return qualityUseCases(repo=repo)