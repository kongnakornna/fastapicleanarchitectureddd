# quality/infrastructure/repositories.py from app.core.logging import logger from app.shared.exceptions import InfrastructureException   class qualityRepositoryException(InfrastructureException):     code = "qc_REPO_ERROR"   class PostgresqualityRepository:     """Postgres repository for quality (2-branch error handling)."""      def __init__(self, session, tenant_id: str):         self.session = session         self.tenant_id = tenant_id