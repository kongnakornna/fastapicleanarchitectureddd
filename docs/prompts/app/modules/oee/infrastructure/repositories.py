# oee/infrastructure/repositories.py from app.core.logging import logger from app.shared.exceptions import InfrastructureException   class oeeRepositoryException(InfrastructureException):     code = "oee_REPO_ERROR"   class PostgresoeeRepository:     """Postgres repository for oee (2-branch error handling)."""      def __init__(self, session, tenant_id: str):         self.session = session         self.tenant_id = tenant_id