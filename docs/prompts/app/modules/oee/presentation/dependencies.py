# oee/presentation/dependencies.py from fastapi import Depends  from app.shared.database import get_session from ..application.use_cases import oeeUseCases from ..infrastructure.repositories import PostgresoeeRepository   async def get_oee_use_cases(     session=Depends(get_session), ) -> oeeUseCases:     """DI provider for oee use cases."""     tenant_id = "00000000-0000-0000-0000-000000000001"  # TODO: from tenant context     repo = PostgresoeeRepository(session, tenant_id)     return oeeUseCases(repo=repo)