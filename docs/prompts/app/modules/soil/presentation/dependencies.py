# soil/presentation/dependencies.py from fastapi import Depends  from app.shared.database import get_session from ..application.use_cases import soilUseCases from ..infrastructure.repositories import PostgressoilRepository   async def get_soil_use_cases(     session=Depends(get_session), ) -> soilUseCases:     """DI provider for soil use cases."""     tenant_id = "00000000-0000-0000-0000-000000000001"  # TODO: from tenant context     repo = PostgressoilRepository(session, tenant_id)     return soilUseCases(repo=repo)