# line_channel/presentation/dependencies.py from fastapi import Depends  from app.shared.database import get_session from ..application.use_cases import line_channelUseCases from ..infrastructure.repositories import Postgresline_channelRepository   async def get_line_channel_use_cases(     session=Depends(get_session), ) -> line_channelUseCases:     """DI provider for line_channel use cases."""     tenant_id = "00000000-0000-0000-0000-000000000001"  # TODO: from tenant context     repo = Postgresline_channelRepository(session, tenant_id)     return line_channelUseCases(repo=repo)