# pos/presentation/dependencies.py from fastapi import Depends  from app.shared.database import get_session from ..application.use_cases import posUseCases from ..infrastructure.repositories import PostgresposRepository   async def get_pos_use_cases(     session=Depends(get_session), ) -> posUseCases:     """DI provider for pos use cases."""     tenant_id = "00000000-0000-0000-0000-000000000001"  # TODO: from tenant context     repo = PostgresposRepository(session, tenant_id)     return posUseCases(repo=repo)