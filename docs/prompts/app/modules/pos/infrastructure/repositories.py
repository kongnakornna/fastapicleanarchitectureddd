# pos/infrastructure/repositories.py from app.core.logging import logger from app.shared.exceptions import InfrastructureException   class posRepositoryException(InfrastructureException):     code = "pos_REPO_ERROR"   class PostgresposRepository:     """Postgres repository for pos (2-branch error handling)."""      def __init__(self, session, tenant_id: str):         self.session = session         self.tenant_id = tenant_id