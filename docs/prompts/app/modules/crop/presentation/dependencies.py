# crop/presentation/dependencies.py from fastapi import Depends  from app.shared.database import get_session from ..application.use_cases import cropUseCases from ..infrastructure.repositories import PostgrescropRepository   async def get_crop_use_cases(     session=Depends(get_session), ) -> cropUseCases:     """DI provider for crop use cases."""     tenant_id = "00000000-0000-0000-0000-000000000001"  # TODO: from tenant context     repo = PostgrescropRepository(session, tenant_id)     return cropUseCases(repo=repo)