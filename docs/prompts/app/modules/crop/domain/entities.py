# crop/domain/entities.py # Entities: Crop, Variety from dataclasses import dataclass from datetime import datetime, timezone  from app.shared.base_entity import BaseEntity  from .exceptions import DomainError   def _utcnow() -> datetime:     return datetime.now(timezone.utc)   # TODO: Add domain entities for: Crop, Variety # Example: # # @dataclass # class cropEntity(BaseEntity): #     code: str = "" #     name: str = "" # #     def __post_init__(self): #         self._validate() # #     def _validate(self) -> None: #         if not self.code: #             raise DomainError("Code is required")