# crop/infrastructure/repositories.py from app.core.logging import logger from app.shared.exceptions import InfrastructureException   class cropRepositoryException(InfrastructureException):     code = "crp_REPO_ERROR"   class PostgrescropRepository:     """Postgres repository for crop (2-branch error handling)."""      def __init__(self, session, tenant_id: str):         self.session = session         self.tenant_id = tenant_id