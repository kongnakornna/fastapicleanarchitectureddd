# shift/presentation/dependencies.py from fastapi import Depends  from app.shared.database import get_session from ..application.use_cases import shiftUseCases from ..infrastructure.repositories import PostgresshiftRepository   async def get_shift_use_cases(     session=Depends(get_session), ) -> shiftUseCases:     """DI provider for shift use cases."""     tenant_id = "00000000-0000-0000-0000-000000000001"  # TODO: from tenant context     repo = PostgresshiftRepository(session, tenant_id)     return shiftUseCases(repo=repo)