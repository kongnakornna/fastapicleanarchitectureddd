# shift/infrastructure/repositories.py from app.core.logging import logger from app.shared.exceptions import InfrastructureException   class shiftRepositoryException(InfrastructureException):     code = "shf_REPO_ERROR"   class PostgresshiftRepository:     """Postgres repository for shift (2-branch error handling)."""      def __init__(self, session, tenant_id: str):         self.session = session         self.tenant_id = tenant_id