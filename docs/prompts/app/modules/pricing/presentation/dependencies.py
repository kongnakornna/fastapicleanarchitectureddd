# pricing/presentation/dependencies.py from fastapi import Depends  from app.shared.database import get_session from ..application.use_cases import pricingUseCases from ..infrastructure.repositories import PostgrespricingRepository   async def get_pricing_use_cases(     session=Depends(get_session), ) -> pricingUseCases:     """DI provider for pricing use cases."""     tenant_id = "00000000-0000-0000-0000-000000000001"  # TODO: from tenant context     repo = PostgrespricingRepository(session, tenant_id)     return pricingUseCases(repo=repo)