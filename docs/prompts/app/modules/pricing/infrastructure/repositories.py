# pricing/infrastructure/repositories.py from app.core.logging import logger from app.shared.exceptions import InfrastructureException   class pricingRepositoryException(InfrastructureException):     code = "prc_REPO_ERROR"   class PostgrespricingRepository:     """Postgres repository for pricing (2-branch error handling)."""      def __init__(self, session, tenant_id: str):         self.session = session         self.tenant_id = tenant_id