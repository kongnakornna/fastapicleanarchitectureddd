# cctv/infrastructure/repositories.py from app.core.logging import logger from app.shared.exceptions import InfrastructureException   class cctvRepositoryException(InfrastructureException):     code = "cctv_REPO_ERROR"   class PostgrescctvRepository:     """Postgres repository for cctv (2-branch error handling)."""      def __init__(self, session, tenant_id: str):         self.session = session         self.tenant_id = tenant_id