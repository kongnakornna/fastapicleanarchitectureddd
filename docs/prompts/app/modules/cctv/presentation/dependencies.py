# cctv/presentation/dependencies.py from fastapi import Depends  from app.shared.database import get_session from ..application.use_cases import cctvUseCases from ..infrastructure.repositories import PostgrescctvRepository   async def get_cctv_use_cases(     session=Depends(get_session), ) -> cctvUseCases:     """DI provider for cctv use cases."""     tenant_id = "00000000-0000-0000-0000-000000000001"  # TODO: from tenant context     repo = PostgrescctvRepository(session, tenant_id)     return cctvUseCases(repo=repo)