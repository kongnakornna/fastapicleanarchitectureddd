# loyalty/presentation/dependencies.py from fastapi import Depends  from app.shared.database import get_session from ..application.use_cases import loyaltyUseCases from ..infrastructure.repositories import PostgresloyaltyRepository   async def get_loyalty_use_cases(     session=Depends(get_session), ) -> loyaltyUseCases:     """DI provider for loyalty use cases."""     tenant_id = "00000000-0000-0000-0000-000000000001"  # TODO: from tenant context     repo = PostgresloyaltyRepository(session, tenant_id)     return loyaltyUseCases(repo=repo)