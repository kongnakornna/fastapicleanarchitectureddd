# loyalty/infrastructure/repositories.py from app.core.logging import logger from app.shared.exceptions import InfrastructureException   class loyaltyRepositoryException(InfrastructureException):     code = "loy_REPO_ERROR"   class PostgresloyaltyRepository:     """Postgres repository for loyalty (2-branch error handling)."""      def __init__(self, session, tenant_id: str):         self.session = session         self.tenant_id = tenant_id