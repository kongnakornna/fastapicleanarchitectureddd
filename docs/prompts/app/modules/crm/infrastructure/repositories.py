# crm/infrastructure/repositories.py from app.core.logging import logger from app.shared.exceptions import InfrastructureException   class crmRepositoryException(InfrastructureException):     code = "crm_REPO_ERROR"   class PostgrescrmRepository:     """Postgres repository for crm (2-branch error handling)."""      def __init__(self, session, tenant_id: str):         self.session = session         self.tenant_id = tenant_id