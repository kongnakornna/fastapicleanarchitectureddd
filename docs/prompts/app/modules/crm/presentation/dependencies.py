# crm/presentation/dependencies.py from fastapi import Depends  from app.shared.database import get_session from ..application.use_cases import crmUseCases from ..infrastructure.repositories import PostgrescrmRepository   async def get_crm_use_cases(     session=Depends(get_session), ) -> crmUseCases:     """DI provider for crm use cases."""     tenant_id = "00000000-0000-0000-0000-000000000001"  # TODO: from tenant context     repo = PostgrescrmRepository(session, tenant_id)     return crmUseCases(repo=repo)