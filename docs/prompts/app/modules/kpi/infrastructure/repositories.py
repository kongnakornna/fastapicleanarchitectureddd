# kpi/infrastructure/repositories.py from app.core.logging import logger from app.shared.exceptions import InfrastructureException   class kpiRepositoryException(InfrastructureException):     code = "kpi_REPO_ERROR"   class PostgreskpiRepository:     """Postgres repository for kpi (2-branch error handling)."""      def __init__(self, session, tenant_id: str):         self.session = session         self.tenant_id = tenant_id