# kpi/presentation/dependencies.py from fastapi import Depends  from app.shared.database import get_session from ..application.use_cases import kpiUseCases from ..infrastructure.repositories import PostgreskpiRepository   async def get_kpi_use_cases(     session=Depends(get_session), ) -> kpiUseCases:     """DI provider for kpi use cases."""     tenant_id = "00000000-0000-0000-0000-000000000001"  # TODO: from tenant context     repo = PostgreskpiRepository(session, tenant_id)     return kpiUseCases(repo=repo)