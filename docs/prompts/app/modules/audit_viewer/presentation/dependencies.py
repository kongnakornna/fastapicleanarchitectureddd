# audit_viewer/presentation/dependencies.py from fastapi import Depends  from app.shared.database import get_session from ..application.use_cases import audit_viewerUseCases from ..infrastructure.repositories import Postgresaudit_viewerRepository   async def get_audit_viewer_use_cases(     session=Depends(get_session), ) -> audit_viewerUseCases:     """DI provider for audit_viewer use cases."""     tenant_id = "00000000-0000-0000-0000-000000000001"  # TODO: from tenant context     repo = Postgresaudit_viewerRepository(session, tenant_id)     return audit_viewerUseCases(repo=repo)