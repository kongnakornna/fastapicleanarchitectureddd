# recipe/presentation/dependencies.py from fastapi import Depends  from app.shared.database import get_session from ..application.use_cases import recipeUseCases from ..infrastructure.repositories import PostgresrecipeRepository   async def get_recipe_use_cases(     session=Depends(get_session), ) -> recipeUseCases:     """DI provider for recipe use cases."""     tenant_id = "00000000-0000-0000-0000-000000000001"  # TODO: from tenant context     repo = PostgresrecipeRepository(session, tenant_id)     return recipeUseCases(repo=repo)