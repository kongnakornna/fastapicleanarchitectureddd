# product/infrastructure/repositories.py from app.core.logging import logger from app.shared.exceptions import InfrastructureException   class productRepositoryException(InfrastructureException):     code = "prd_REPO_ERROR"   class PostgresproductRepository:     """Postgres repository for product (2-branch error handling)."""      def __init__(self, session, tenant_id: str):         self.session = session         self.tenant_id = tenant_id