# product/presentation/dependencies.py from fastapi import Depends  from app.shared.database import get_session from ..application.use_cases import productUseCases from ..infrastructure.repositories import PostgresproductRepository   async def get_product_use_cases(     session=Depends(get_session), ) -> productUseCases:     """DI provider for product use cases."""     tenant_id = "00000000-0000-0000-0000-000000000001"  # TODO: from tenant context     repo = PostgresproductRepository(session, tenant_id)     return productUseCases(repo=repo)