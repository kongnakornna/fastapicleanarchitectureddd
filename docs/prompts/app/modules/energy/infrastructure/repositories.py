# energy/infrastructure/repositories.py from app.core.logging import logger from app.shared.exceptions import InfrastructureException   class energyRepositoryException(InfrastructureException):     code = "eng_REPO_ERROR"   class PostgresenergyRepository:     """Postgres repository for energy (2-branch error handling)."""      def __init__(self, session, tenant_id: str):         self.session = session         self.tenant_id = tenant_id