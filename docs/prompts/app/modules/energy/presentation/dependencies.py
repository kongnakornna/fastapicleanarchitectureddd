# energy/presentation/dependencies.py from fastapi import Depends  from app.shared.database import get_session from ..application.use_cases import energyUseCases from ..infrastructure.repositories import PostgresenergyRepository   async def get_energy_use_cases(     session=Depends(get_session), ) -> energyUseCases:     """DI provider for energy use cases."""     tenant_id = "00000000-0000-0000-0000-000000000001"  # TODO: from tenant context     repo = PostgresenergyRepository(session, tenant_id)     return energyUseCases(repo=repo)