# tax/infrastructure/repositories.py from app.core.logging import logger from app.shared.exceptions import InfrastructureException   class taxRepositoryException(InfrastructureException):     code = "tax_REPO_ERROR"   class PostgrestaxRepository:     """Postgres repository for tax (2-branch error handling)."""      def __init__(self, session, tenant_id: str):         self.session = session         self.tenant_id = tenant_id