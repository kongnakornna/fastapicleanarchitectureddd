# tax/presentation/dependencies.py from fastapi import Depends  from app.shared.database import get_session from ..application.use_cases import taxUseCases from ..infrastructure.repositories import PostgrestaxRepository   async def get_tax_use_cases(     session=Depends(get_session), ) -> taxUseCases:     """DI provider for tax use cases."""     tenant_id = "00000000-0000-0000-0000-000000000001"  # TODO: from tenant context     repo = PostgrestaxRepository(session, tenant_id)     return taxUseCases(repo=repo)