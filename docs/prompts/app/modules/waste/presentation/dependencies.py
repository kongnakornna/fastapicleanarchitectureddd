# waste/presentation/dependencies.py from fastapi import Depends  from app.shared.database import get_session from ..application.use_cases import wasteUseCases from ..infrastructure.repositories import PostgreswasteRepository   async def get_waste_use_cases(     session=Depends(get_session), ) -> wasteUseCases:     """DI provider for waste use cases."""     tenant_id = "00000000-0000-0000-0000-000000000001"  # TODO: from tenant context     repo = PostgreswasteRepository(session, tenant_id)     return wasteUseCases(repo=repo)