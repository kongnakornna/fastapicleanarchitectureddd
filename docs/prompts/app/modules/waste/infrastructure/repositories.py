# waste/infrastructure/repositories.py from app.core.logging import logger from app.shared.exceptions import InfrastructureException   class wasteRepositoryException(InfrastructureException):     code = "wst_REPO_ERROR"   class PostgreswasteRepository:     """Postgres repository for waste (2-branch error handling)."""      def __init__(self, session, tenant_id: str):         self.session = session         self.tenant_id = tenant_id