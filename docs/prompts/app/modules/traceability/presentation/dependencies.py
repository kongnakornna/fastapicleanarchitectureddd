# traceability/presentation/dependencies.py from fastapi import Depends  from app.shared.database import get_session from ..application.use_cases import traceabilityUseCases from ..infrastructure.repositories import PostgrestraceabilityRepository   async def get_traceability_use_cases(     session=Depends(get_session), ) -> traceabilityUseCases:     """DI provider for traceability use cases."""     tenant_id = "00000000-0000-0000-0000-000000000001"  # TODO: from tenant context     repo = PostgrestraceabilityRepository(session, tenant_id)     return traceabilityUseCases(repo=repo)