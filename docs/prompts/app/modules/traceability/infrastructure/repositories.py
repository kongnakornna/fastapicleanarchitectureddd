# traceability/infrastructure/repositories.py from app.core.logging import logger from app.shared.exceptions import InfrastructureException   class traceabilityRepositoryException(InfrastructureException):     code = "trc_REPO_ERROR"   class PostgrestraceabilityRepository:     """Postgres repository for traceability (2-branch error handling)."""      def __init__(self, session, tenant_id: str):         self.session = session         self.tenant_id = tenant_id