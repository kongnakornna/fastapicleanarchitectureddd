# retail/presentation/dependencies.py from fastapi import Depends  from app.shared.database import get_session from ..application.use_cases import retailUseCases from ..infrastructure.repositories import PostgresretailRepository   async def get_retail_use_cases(     session=Depends(get_session), ) -> retailUseCases:     """DI provider for retail use cases."""     tenant_id = "00000000-0000-0000-0000-000000000001"  # TODO: from tenant context     repo = PostgresretailRepository(session, tenant_id)     return retailUseCases(repo=repo)