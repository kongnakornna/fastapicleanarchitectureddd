# delivery/presentation/dependencies.py from fastapi import Depends  from app.shared.database import get_session from ..application.use_cases import deliveryUseCases from ..infrastructure.repositories import PostgresdeliveryRepository   async def get_delivery_use_cases(     session=Depends(get_session), ) -> deliveryUseCases:     """DI provider for delivery use cases."""     tenant_id = "00000000-0000-0000-0000-000000000001"  # TODO: from tenant context     repo = PostgresdeliveryRepository(session, tenant_id)     return deliveryUseCases(repo=repo)