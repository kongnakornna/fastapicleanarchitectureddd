# reconciliation/presentation/dependencies.py from fastapi import Depends  from app.shared.database import get_session from ..application.use_cases import reconciliationUseCases from ..infrastructure.repositories import PostgresreconciliationRepository   async def get_reconciliation_use_cases(     session=Depends(get_session), ) -> reconciliationUseCases:     """DI provider for reconciliation use cases."""     tenant_id = "00000000-0000-0000-0000-000000000001"  # TODO: from tenant context     repo = PostgresreconciliationRepository(session, tenant_id)     return reconciliationUseCases(repo=repo)