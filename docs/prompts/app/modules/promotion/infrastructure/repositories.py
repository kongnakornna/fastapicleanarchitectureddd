# promotion/infrastructure/repositories.py from app.core.logging import logger from app.shared.exceptions import InfrastructureException   class promotionRepositoryException(InfrastructureException):     code = "promo_REPO_ERROR"   class PostgrespromotionRepository:     """Postgres repository for promotion (2-branch error handling)."""      def __init__(self, session, tenant_id: str):         self.session = session         self.tenant_id = tenant_id