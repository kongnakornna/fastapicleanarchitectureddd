# promotion/presentation/dependencies.py from fastapi import Depends  from app.shared.database import get_session from ..application.use_cases import promotionUseCases from ..infrastructure.repositories import PostgrespromotionRepository   async def get_promotion_use_cases(     session=Depends(get_session), ) -> promotionUseCases:     """DI provider for promotion use cases."""     tenant_id = "00000000-0000-0000-0000-000000000001"  # TODO: from tenant context     repo = PostgrespromotionRepository(session, tenant_id)     return promotionUseCases(repo=repo)