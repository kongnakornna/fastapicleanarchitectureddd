# route/presentation/dependencies.py from fastapi import Depends  from app.shared.database import get_session from ..application.use_cases import routeUseCases from ..infrastructure.repositories import PostgresrouteRepository   async def get_route_use_cases(     session=Depends(get_session), ) -> routeUseCases:     """DI provider for route use cases."""     tenant_id = "00000000-0000-0000-0000-000000000001"  # TODO: from tenant context     repo = PostgresrouteRepository(session, tenant_id)     return routeUseCases(repo=repo)