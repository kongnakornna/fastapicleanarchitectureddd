# route/infrastructure/repositories.py from app.core.logging import logger from app.shared.exceptions import InfrastructureException   class routeRepositoryException(InfrastructureException):     code = "rte_REPO_ERROR"   class PostgresrouteRepository:     """Postgres repository for route (2-branch error handling)."""      def __init__(self, session, tenant_id: str):         self.session = session         self.tenant_id = tenant_id