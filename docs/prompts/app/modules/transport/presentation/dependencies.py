# transport/presentation/dependencies.py from fastapi import Depends  from app.shared.database import get_session from ..application.use_cases import transportUseCases from ..infrastructure.repositories import PostgrestransportRepository   async def get_transport_use_cases(     session=Depends(get_session), ) -> transportUseCases:     """DI provider for transport use cases."""     tenant_id = "00000000-0000-0000-0000-000000000001"  # TODO: from tenant context     repo = PostgrestransportRepository(session, tenant_id)     return transportUseCases(repo=repo)