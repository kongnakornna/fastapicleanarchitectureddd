# transport/infrastructure/repositories.py from app.core.logging import logger from app.shared.exceptions import InfrastructureException   class transportRepositoryException(InfrastructureException):     code = "trn_REPO_ERROR"   class PostgrestransportRepository:     """Postgres repository for transport (2-branch error handling)."""      def __init__(self, session, tenant_id: str):         self.session = session         self.tenant_id = tenant_id