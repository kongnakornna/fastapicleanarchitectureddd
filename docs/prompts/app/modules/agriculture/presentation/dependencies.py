# agriculture/presentation/dependencies.py from fastapi import Depends  from app.shared.database import get_session from ..application.use_cases import agricultureUseCases from ..infrastructure.repositories import PostgresagricultureRepository   async def get_agriculture_use_cases(     session=Depends(get_session), ) -> agricultureUseCases:     """DI provider for agriculture use cases."""     tenant_id = "00000000-0000-0000-0000-000000000001"  # TODO: from tenant context     repo = PostgresagricultureRepository(session, tenant_id)     return agricultureUseCases(repo=repo)