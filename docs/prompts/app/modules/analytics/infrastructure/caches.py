# analytics/infrastructure/caches.py # Cache never raises. from app.core.logging import logger   class RedisanalyticsCache:     """Redis cache for analytics (never-raise)."""      def __init__(self, redis_client):         self.redis = redis_client      async def get(self, key: str):         try:             return await self.redis.get(f"anl:{key}")         except Exception as e:             logger.warning("anl.cache.get.failed", error=str(e))             return None      async def set(self, key: str, value: str, ttl: int = 300) -> None:         try:             await self.redis.setex(f"anl:{key}", ttl, value)         except Exception as e:             logger.warning("anl.cache.set.failed", error=str(e))      async def delete(self, key: str) -> None:         try:             await self.redis.delete(f"anl:{key}")         except Exception as e:             logger.warning("anl.cache.del.failed", error=str(e))