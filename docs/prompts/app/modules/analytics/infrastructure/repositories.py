# analytics/infrastructure/repositories.py from app.core.logging import logger from app.shared.exceptions import InfrastructureException   class analyticsRepositoryException(InfrastructureException):     code = "anl_REPO_ERROR"   class PostgresanalyticsRepository:     """Postgres repository for analytics (2-branch error handling)."""      def __init__(self, session, tenant_id: str):         self.session = session         self.tenant_id = tenant_id