# analytics/presentation/dependencies.py from fastapi import Depends  from app.shared.database import get_session from ..application.use_cases import analyticsUseCases from ..infrastructure.repositories import PostgresanalyticsRepository   async def get_analytics_use_cases(     session=Depends(get_session), ) -> analyticsUseCases:     """DI provider for analytics use cases."""     tenant_id = "00000000-0000-0000-0000-000000000001"  # TODO: from tenant context     repo = PostgresanalyticsRepository(session, tenant_id)     return analyticsUseCases(repo=repo)