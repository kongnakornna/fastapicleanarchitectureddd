# deviceiot/presentation/dependencies.py from fastapi import Depends  from app.shared.database import get_session from ..application.use_cases import deviceiotUseCases from ..infrastructure.repositories import PostgresdeviceiotRepository   async def get_deviceiot_use_cases(     session=Depends(get_session), ) -> deviceiotUseCases:     """DI provider for deviceiot use cases."""     tenant_id = "00000000-0000-0000-0000-000000000001"  # TODO: from tenant context     repo = PostgresdeviceiotRepository(session, tenant_id)     return deviceiotUseCases(repo=repo)