# deviceiot/infrastructure/repositories.py from app.core.logging import logger from app.shared.exceptions import InfrastructureException   class deviceiotRepositoryException(InfrastructureException):     code = "deviceiot_REPO_ERROR"   class PostgresdeviceiotRepository:     """Postgres repository for deviceiot (2-branch error handling)."""      def __init__(self, session, tenant_id: str):         self.session = session         self.tenant_id = tenant_id