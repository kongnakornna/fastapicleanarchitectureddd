# inventory/presentation/dependencies.py from fastapi import Depends  from app.shared.database import get_session from ..application.use_cases import inventoryUseCases from ..infrastructure.repositories import PostgresinventoryRepository   async def get_inventory_use_cases(     session=Depends(get_session), ) -> inventoryUseCases:     """DI provider for inventory use cases."""     tenant_id = "00000000-0000-0000-0000-000000000001"  # TODO: from tenant context     repo = PostgresinventoryRepository(session, tenant_id)     return inventoryUseCases(repo=repo)