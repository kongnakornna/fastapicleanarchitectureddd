# inventory/infrastructure/repositories.py from app.core.logging import logger from app.shared.exceptions import InfrastructureException   class inventoryRepositoryException(InfrastructureException):     code = "invt_REPO_ERROR"   class PostgresinventoryRepository:     """Postgres repository for inventory (2-branch error handling)."""      def __init__(self, session, tenant_id: str):         self.session = session         self.tenant_id = tenant_id