# customer/presentation/dependencies.py from fastapi import Depends  from app.shared.database import get_session from ..application.use_cases import customerUseCases from ..infrastructure.repositories import PostgrescustomerRepository   async def get_customer_use_cases(     session=Depends(get_session), ) -> customerUseCases:     """DI provider for customer use cases."""     tenant_id = "00000000-0000-0000-0000-000000000001"  # TODO: from tenant context     repo = PostgrescustomerRepository(session, tenant_id)     return customerUseCases(repo=repo)