# customer/infrastructure/repositories.py from app.core.logging import logger from app.shared.exceptions import InfrastructureException   class customerRepositoryException(InfrastructureException):     code = "cus_REPO_ERROR"   class PostgrescustomerRepository:     """Postgres repository for customer (2-branch error handling)."""      def __init__(self, session, tenant_id: str):         self.session = session         self.tenant_id = tenant_id