# support/presentation/dependencies.py from fastapi import Depends  from app.shared.database import get_session from ..application.use_cases import supportUseCases from ..infrastructure.repositories import PostgressupportRepository   async def get_support_use_cases(     session=Depends(get_session), ) -> supportUseCases:     """DI provider for support use cases."""     tenant_id = "00000000-0000-0000-0000-000000000001"  # TODO: from tenant context     repo = PostgressupportRepository(session, tenant_id)     return supportUseCases(repo=repo)