# support/infrastructure/repositories.py from app.core.logging import logger from app.shared.exceptions import InfrastructureException   class supportRepositoryException(InfrastructureException):     code = "sup2_REPO_ERROR"   class PostgressupportRepository:     """Postgres repository for support (2-branch error handling)."""      def __init__(self, session, tenant_id: str):         self.session = session         self.tenant_id = tenant_id