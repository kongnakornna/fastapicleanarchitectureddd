# alerting/presentation/dependencies.py from fastapi import Depends  from app.shared.database import get_session from ..application.use_cases import alertingUseCases from ..infrastructure.repositories import PostgresalertingRepository   async def get_alerting_use_cases(     session=Depends(get_session), ) -> alertingUseCases:     """DI provider for alerting use cases."""     tenant_id = "00000000-0000-0000-0000-000000000001"  # TODO: from tenant context     repo = PostgresalertingRepository(session, tenant_id)     return alertingUseCases(repo=repo)