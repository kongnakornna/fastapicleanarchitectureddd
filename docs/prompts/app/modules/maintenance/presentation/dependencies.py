# maintenance/presentation/dependencies.py from fastapi import Depends  from app.shared.database import get_session from ..application.use_cases import maintenanceUseCases from ..infrastructure.repositories import PostgresmaintenanceRepository   async def get_maintenance_use_cases(     session=Depends(get_session), ) -> maintenanceUseCases:     """DI provider for maintenance use cases."""     tenant_id = "00000000-0000-0000-0000-000000000001"  # TODO: from tenant context     repo = PostgresmaintenanceRepository(session, tenant_id)     return maintenanceUseCases(repo=repo)