# production/infrastructure/repositories.py from app.core.logging import logger from app.shared.exceptions import InfrastructureException   class productionRepositoryException(InfrastructureException):     code = "prod_REPO_ERROR"   class PostgresproductionRepository:     """Postgres repository for production (2-branch error handling)."""      def __init__(self, session, tenant_id: str):         self.session = session         self.tenant_id = tenant_id