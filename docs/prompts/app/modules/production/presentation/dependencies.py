# production/presentation/dependencies.py from fastapi import Depends  from app.shared.database import get_session from ..application.use_cases import productionUseCases from ..infrastructure.repositories import PostgresproductionRepository   async def get_production_use_cases(     session=Depends(get_session), ) -> productionUseCases:     """DI provider for production use cases."""     tenant_id = "00000000-0000-0000-0000-000000000001"  # TODO: from tenant context     repo = PostgresproductionRepository(session, tenant_id)     return productionUseCases(repo=repo)