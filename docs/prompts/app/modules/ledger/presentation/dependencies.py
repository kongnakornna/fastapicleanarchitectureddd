# ledger/presentation/dependencies.py from fastapi import Depends  from app.shared.database import get_session from ..application.use_cases import ledgerUseCases from ..infrastructure.repositories import PostgresledgerRepository   async def get_ledger_use_cases(     session=Depends(get_session), ) -> ledgerUseCases:     """DI provider for ledger use cases."""     tenant_id = "00000000-0000-0000-0000-000000000001"  # TODO: from tenant context     repo = PostgresledgerRepository(session, tenant_id)     return ledgerUseCases(repo=repo)