# ledger/presentation/routers.py from fastapi import APIRouter, Depends  from .dependencies import get_ledger_use_cases  router = APIRouter(prefix="/api/v1/ledger", tags=["ledger"])   # TODO: Add endpoints