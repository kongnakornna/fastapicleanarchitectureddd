# reporting/presentation/dependencies.py from fastapi import Depends  from app.shared.database import get_session from ..application.use_cases import reportingUseCases from ..infrastructure.repositories import PostgresreportingRepository   async def get_reporting_use_cases(     session=Depends(get_session), ) -> reportingUseCases:     """DI provider for reporting use cases."""     tenant_id = "00000000-0000-0000-0000-000000000001"  # TODO: from tenant context     repo = PostgresreportingRepository(session, tenant_id)     return reportingUseCases(repo=repo)