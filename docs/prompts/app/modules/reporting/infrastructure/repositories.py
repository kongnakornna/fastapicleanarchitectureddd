# reporting/infrastructure/repositories.py from app.core.logging import logger from app.shared.exceptions import InfrastructureException   class reportingRepositoryException(InfrastructureException):     code = "rpt_REPO_ERROR"   class PostgresreportingRepository:     """Postgres repository for reporting (2-branch error handling)."""      def __init__(self, session, tenant_id: str):         self.session = session         self.tenant_id = tenant_id