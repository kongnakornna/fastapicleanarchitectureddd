# blank/infrastructure/repositories.py from app.core.logging import logger from app.shared.exceptions import InfrastructureException   class blankRepositoryException(InfrastructureException):     code = "blk_REPO_ERROR"   class PostgresblankRepository:     """Postgres repository for blank (2-branch error handling)."""      def __init__(self, session, tenant_id: str):         self.session = session         self.tenant_id = tenant_id