# blank/presentation/dependencies.py from fastapi import Depends  from app.shared.database import get_session from ..application.use_cases import blankUseCases from ..infrastructure.repositories import PostgresblankRepository   async def get_blank_use_cases(     session=Depends(get_session), ) -> blankUseCases:     """DI provider for blank use cases."""     tenant_id = "00000000-0000-0000-0000-000000000001"  # TODO: from tenant context     repo = PostgresblankRepository(session, tenant_id)     return blankUseCases(repo=repo)