# iot/infrastructure/repositories.py from app.core.logging import logger from app.shared.exceptions import InfrastructureException   class iotRepositoryException(InfrastructureException):     code = "iot_REPO_ERROR"   class PostgresiotRepository:     """Postgres repository for iot (2-branch error handling)."""      def __init__(self, session, tenant_id: str):         self.session = session         self.tenant_id = tenant_id