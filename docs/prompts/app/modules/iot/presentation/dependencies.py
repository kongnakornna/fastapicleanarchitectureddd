# iot/presentation/dependencies.py from fastapi import Depends  from app.shared.database import get_session from ..application.use_cases import iotUseCases from ..infrastructure.repositories import PostgresiotRepository   async def get_iot_use_cases(     session=Depends(get_session), ) -> iotUseCases:     """DI provider for iot use cases."""     tenant_id = "00000000-0000-0000-0000-000000000001"  # TODO: from tenant context     repo = PostgresiotRepository(session, tenant_id)     return iotUseCases(repo=repo)