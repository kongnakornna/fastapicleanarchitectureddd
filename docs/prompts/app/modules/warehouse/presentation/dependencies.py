# warehouse/presentation/dependencies.py from fastapi import Depends  from app.shared.database import get_session from ..application.use_cases import warehouseUseCases from ..infrastructure.repositories import PostgreswarehouseRepository   async def get_warehouse_use_cases(     session=Depends(get_session), ) -> warehouseUseCases:     """DI provider for warehouse use cases."""     tenant_id = "00000000-0000-0000-0000-000000000001"  # TODO: from tenant context     repo = PostgreswarehouseRepository(session, tenant_id)     return warehouseUseCases(repo=repo)