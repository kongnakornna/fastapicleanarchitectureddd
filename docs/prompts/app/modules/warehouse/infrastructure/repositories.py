# warehouse/infrastructure/repositories.py from app.core.logging import logger from app.shared.exceptions import InfrastructureException   class warehouseRepositoryException(InfrastructureException):     code = "wh_REPO_ERROR"   class PostgreswarehouseRepository:     """Postgres repository for warehouse (2-branch error handling)."""      def __init__(self, session, tenant_id: str):         self.session = session         self.tenant_id = tenant_id