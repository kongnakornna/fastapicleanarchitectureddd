# employee/infrastructure/repositories.py from app.core.logging import logger from app.shared.exceptions import InfrastructureException   class employeeRepositoryException(InfrastructureException):     code = "emp_REPO_ERROR"   class PostgresemployeeRepository:     """Postgres repository for employee (2-branch error handling)."""      def __init__(self, session, tenant_id: str):         self.session = session         self.tenant_id = tenant_id