# employee/presentation/dependencies.py from fastapi import Depends  from app.shared.database import get_session from ..application.use_cases import employeeUseCases from ..infrastructure.repositories import PostgresemployeeRepository   async def get_employee_use_cases(     session=Depends(get_session), ) -> employeeUseCases:     """DI provider for employee use cases."""     tenant_id = "00000000-0000-0000-0000-000000000001"  # TODO: from tenant context     repo = PostgresemployeeRepository(session, tenant_id)     return employeeUseCases(repo=repo)