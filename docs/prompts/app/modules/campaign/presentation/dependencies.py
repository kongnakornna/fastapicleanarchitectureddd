# campaign/presentation/dependencies.py from fastapi import Depends  from app.shared.database import get_session from ..application.use_cases import campaignUseCases from ..infrastructure.repositories import PostgrescampaignRepository   async def get_campaign_use_cases(     session=Depends(get_session), ) -> campaignUseCases:     """DI provider for campaign use cases."""     tenant_id = "00000000-0000-0000-0000-000000000001"  # TODO: from tenant context     repo = PostgrescampaignRepository(session, tenant_id)     return campaignUseCases(repo=repo)