# campaign/infrastructure/repositories.py from app.core.logging import logger from app.shared.exceptions import InfrastructureException   class campaignRepositoryException(InfrastructureException):     code = "cmp_REPO_ERROR"   class PostgrescampaignRepository:     """Postgres repository for campaign (2-branch error handling)."""      def __init__(self, session, tenant_id: str):         self.session = session         self.tenant_id = tenant_id